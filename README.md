# 加把劲儿

# TODO

## 注册页面
1. 修改 `showAlert()`方法，让不同的状态显示不同的提示信息
2. `<input>`信息验证错误的时候，加上**红色边框**提示
3. 页面数据绑定

## 总体
1. 抽离出共同特点的方法，放在独立的文件中

# 笔记

## 登录页面
1. 个人觉得登录页面的`<input>`不需要使用`blur`来验证，因为有登录过的用户是知道 username 的格式的。注册页面则需要。

## 注册页面
1. 如果按钮“前进”和“后退”使用数字的加减来控制，看起来行，但是如果多次按下按钮，将会出现更大的值或者是负值。所以还是用一个固定的值(one two three)


# 遇到问题

1. 在使用原生JavaScript的时候如何进行页面数据绑定
2. 注册页面左上角三角形卡片返回标签效果（图形和文字使用兄弟元素试试）
